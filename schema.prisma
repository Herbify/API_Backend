datasource db {
    provider = "mysql"
    // mysql://username:password@localhost:port/database
    url      = "mysql://root:@localhost:3306/herbify"
}

generator client {
    provider = "prisma-client-js"
}

model User {
    id         Int      @id @default(autoincrement())
    name       String
    photo      String
    email      String   @unique
    password   String
    status     Int      @default(0)
    verifiedAt DateTime
    createdAt  DateTime @default(now())
    updatedAt  DateTime @updatedAt
    Otp        Otp[]
}

model Otp {
    id        Int      @id @default(autoincrement())
    code      Int?
    userId    Int
    expiredAt DateTime
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    user      User     @relation(fields: [userId], references: [id])
}

model Articles {
    id        Int      @id @default(autoincrement())
    idUser    Int
    title     String
    photo     String?
    content   String
    tag       Json?
    createdAt DateTime @default(now())
    updatedAt DateTime 
}

model Products {
    id         Int      @id @default(autoincrement())
    name       String
    ingredient Json
    price      Int
    location   String
    url        String?
    photo      String
    createdAt  DateTime @default(now())
    updatedAt  DateTime 
}

model UserActivities {
    id        Int      @id @default(autoincrement())
    userId    Int
    type      String
    activity  Json
    status    Int
    createdAt DateTime @default(now())
    updatedAt DateTime 
}

model Articles {
    id        Int      @id @default(autoincrement())
    idUser    Int
    title     String
    photo     String?
    content   String
    tag       Json?
    createdAt DateTime @default(now())
    updatedAt DateTime 
}

model Products {
    id         Int      @id @default(autoincrement())
    name       String
    ingredient Json
    price      Int
    location   String
    url        String?
    photo      String
    createdAt  DateTime @default(now())
    updatedAt  DateTime 
}

model UserActivities {
    id        Int      @id @default(autoincrement())
    userId    Int
    type      String
    activity  Json
    status    Int
    createdAt DateTime @default(now())
    updatedAt DateTime 
}
